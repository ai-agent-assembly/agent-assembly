# Downloaded guest images — reproducible via scripts/fetch-images.sh with
# pinned+verified checksums, not committed (large binaries, per repo policy).
images/

# Swift build output
.build/
*.o
*.swiftmodule

# Console capture evidence from local runs
boot-console.log
virtiofs-vsock-hostside-console.log

# guest-init cross-compile output (cargo build artifacts for the guest
# aarch64-unknown-linux-musl binary — see scripts/build-guest-init.sh)
guest-init/target/
