/**
 * Truthfulness renderers (AAASM-5173). Import these rather than hand-rolling a
 * placeholder — see `src/lib/truthfulness` for the vocabulary they render.
 */
export { AbsenceMarker, TruthfulValue, type AbsenceMarkerProps, type TruthfulValueProps } from './TruthfulValue'
export { StatusState, type StatusStateProps } from './StatusState'
