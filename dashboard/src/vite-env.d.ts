/// <reference types="vite/client" />

/**
 * Dashboard build version, injected by Vite `define` from package.json
 * (see vite.config.ts). Used by the AppShell brand sub-line (AAASM-5021).
 */
declare const __APP_VERSION__: string
