# Security sign-off — v0.0.1-rc.7

- **Version:** v0.0.1-rc.7
- **Release type:** patch (rc.6 → rc.7, same pre-release channel, trailing counter advances)
- **Previous tag:** v0.0.1-rc.6
- **Candidate SHA:** 37317d6fe43b317e281ee7f919a25d58c4accf16
- **Reviewer:** Claude Sonnet 5 (agent), founder-directed re-validation
- **Date:** 2026-09-17

## Candidate SHA advanced again (`eb0242126` → `37317d6fe`)

While AAASM-6091's tag-cut was blocked on a real defect in
`release-tag-guard.sh`'s `--strict-tag-binding` check (fixed by PR #2445,
see `docs/src/adr/0037-release-candidate-tag-binding-and-evidence-attempt-identity.md`
revision 4), 26 further commits landed on
`main` — genuinely new material, not release-mechanical churn:

- **PR #2445** (this campaign's own guard fix) — Rust release-tooling
  logic (`scripts/qa/check-release-evidence.py`) + tests, independently
  adversarially reviewed (see the PR's review comments) before merge.
  Not product/runtime code.
- **13 routine Dependabot version bumps** (console, open, uuid, toml,
  jsonschema ×2, wasmtime-wasi, wat, bitflags, react-dom,
  protoc-gen-es, plus the DI-API bindings this last one regenerates) —
  each individually CI-green, no scope expansion beyond the version
  literal, auto-merged per this repo's standard Bot PR Policy.
- **PR #2465** (`rustls 0.23.44 → 0.23.45`, `b9f1e9985`) — fixes
  `RUSTSEC-2026-0285` (TLS 1.3 handshake messages incorrectly accepted
  across encryption-level boundaries), an advisory published *after*
  the prior candidate was captured, which had turned `main`'s own
  `cargo deny check advisories` red with no code change on this repo's
  part. **Net effect on this sign-off: strictly improves the security
  posture** — the vulnerable version is no longer in the dependency
  tree.
- **PR #2466** (`aa-integration-tests/tests/fixtures/e2e/sdk_go_driver/go.mod`
  retidy, AAASM-6111) — test-fixture-only, not product code.

`cargo deny check advisories`, Dependabot, and CodeQL were all re-queried
fresh against `37317d6fe` (see updated counts below), not assumed
unchanged from `eb0242126`.

## Candidate SHA advanced since this sign-off's original write (`f6183571d` → `eb0242126`)

Nine commits landed after the analysis below was performed, none of which
change the security posture already assessed:
- `fa88f4bf5`..`5f41ac0a7` — this sign-off's own regeneration + a real
  AAASM-6091 test-coverage gap fixed in `aa-cli/tests/integrations_claude_code.rs`
  (test-only, independently reviewed — see PR #2438).
- `482c27855`..`90a18f391` — AAASM-5308/J79's real-device evidence file
  and `qa/golden-journeys.yaml` registry update (docs/registry only, no
  product code — see PR #2440).

`git diff --name-only f6183571d..eb0242126` confirms the only `.rs` file
touched in this range is the test file above; `cargo deny check
advisories`, Dependabot, and CodeQL were all re-queried fresh against
`eb0242126` (see updated counts below) rather than assumed unchanged.

## Why this sign-off was regenerated

Founder decision for AAASM-6091 changed `Engine::repair()`'s mutation
semantics (fail-safe/no-clobber default for an externally-changed
AASM-owned key, PRs #2436/#2437, merged into `f6183571d`). The founder
required a **fresh** re-validation — not reuse of the prior rc.7
sign-off (candidate `dce039704`) — because a change to mutation
semantics is exactly the class of change this gate exists to catch.

## Inputs reviewed

- `cargo deny check advisories` (toolchain 1.98.1, matching CI's resolved
  `stable` for this candidate SHA) — **clean**, `advisories ok`.
- Open Dependabot alerts — **0 open**
  (`gh api repos/ai-agent-assembly/agent-assembly/dependabot/alerts --paginate --jq '[.[] | select(.state=="open")] | length'`).
- Open CodeQL alerts — **0 open**
  (`gh api repos/ai-agent-assembly/agent-assembly/code-scanning/alerts --paginate --jq '[.[] | select(.state=="open")] | length'`).
  The 62 alerts disposed of as REJECTED in the prior rc.7 sign-off
  (candidate `dce039704`) were re-queried fresh here and remain 0
  open/re-introduced.
- Release diff scoped to this sign-off's actual delta — the prior rc.7
  security sign-off already covers `v0.0.1-rc.6..dce039704`
  (4637+ commits, disposed of there). This sign-off's genuinely new
  material is `dce039704..f6183571d`: **24 commits, 28 files**, all
  AAASM-6091 (the fail-safe/no-clobber fix + its independent-review
  follow-up). Re-reviewing the full `v0.0.1-rc.6..HEAD` range line-by-line
  a second time would not surface anything the prior sign-off's disposal
  didn't already cover for the unchanged portion.
- Native `/security-review`-equivalent scan on the `dce039704..f6183571d`
  delta — run as a scoped independent review (general-purpose read-only
  agent) against the full diff of those 28 files, specifically probing
  the new `OwnershipConflict`/`reconcile` override path for a
  scope-broadening or unowned-key-write bypass, and the credential
  screening on the new conflict-disclosure surface. **No high-confidence
  (≥8/10) findings.** See Findings below.
- Independent adversarial review of PR #2445 (`eb0242126..37317d6fe`'s
  only non-dependency, non-test-fixture Rust change) — traced the full
  merge-verification acceptance predicate by hand, attempted to construct
  a bypass, reproduced the fix's real-world effect causally (checked out
  the pre-fix commit and confirmed the guard still correctly refuses
  there), ran the regression suite plus targeted mutation tests
  (deliberately weakening two of the five acceptance conditions in a
  scratch copy and confirming the corresponding test then fails). No
  bypass found. This is release-tooling logic (gates a tag push), not
  product/runtime code reachable by an agent or an external caller — its
  blast radius is this repo's own release process, not a deployed
  system.
- `anthropics/claude-code-security-review` Action — n/a (patch tier;
  major tier only).
- SonarCloud Quality Gate on `f6183571d` — **FAILED** ("C Reliability
  Rating on New Code", required ≥ A). Investigated below; not a
  security finding and not release-blocking.
- SonarCloud analysis on PR #2445 — `skipping` (path-filtered; not a
  gating context on `main`, only `CI Success` is). No new SonarCloud
  finding to disposition for the `eb0242126..37317d6fe` delta.

## Findings

| Severity | Finding | Status |
|---|---|---|
| — | No high-confidence security findings in the `dce039704..f6183571d` delta. `reconcile` can only narrow which already-in-scope, already-repairable steps get their conflict overridden — it cannot broaden scope or target a step/key outside the drift report (`reconcile_naming_an_unaffected_step_changes_nothing` test covers this). Neither the CLI (`aa-cli/.../repair.rs`) nor the DI-API wire handler (`aa-runtime/src/devint/server.rs`) currently exposes `reconcile` to an external caller — both hardcode `&[]` — so the new override is only reachable in-process today; no live remote/CLI trust-boundary bypass is introduced. The credential-screening call (`fingerprint::screen_managed_values`) in the new `conflict_for_step` disclosure reuses the existing, unmodified screen exactly as `PriorSettingsState` already does, and fails safe on any read/parse error. | n/a — none open |
| — | No high-confidence security findings in the `eb0242126..37317d6fe` delta. 13 routine dependency bumps (already dependabot+CI vetted, `cargo deny check advisories` clean at the new versions), one CVE-fixing bump (rustls, strictly improves posture), one test-fixture retidy, and this campaign's own release-tooling guard fix (independently adversarially reviewed, no bypass found — release-process-scoped, not product/runtime code). | n/a — none open |
| Low (code quality, not security) | SonarCloud `rust:S7493` × 2, `aa-devtool-windsurf/src/lib.rs:321,349` — `apply_settings` (rewritten in this delta by the read-merge-write fix, commit `db95c8317`) calls blocking `std::fs::read_to_string`/`std::fs::write` inside an `async fn`. Flagged as "new code" because the function body was rewritten here, failing the repo's Quality Gate ("C Reliability Rating on New Code", required ≥ A). **Investigated: this is the repo's existing cross-adapter convention, not a regression this delta introduced** — `aa-devtool-claude-code/src/lifecycle.rs:462,557`, `aa-devtool-codex/src/lib.rs:307,347`, and `aa-devtool-copilot/src/lib.rs:180,201` all use the identical synchronous-`std::fs`-inside-`async fn` pattern; those instances simply predate this delta's leak period so Sonar doesn't flag them as new. Fixing only `windsurf` here would be inconsistent with its siblings and would reintroduce the same flag on whichever adapter is next touched. Filed as a follow-up to migrate all four adapters to `tokio::fs` together (AAASM-6093) rather than fixed piecemeal in this delta. Not a security defect (no injection/traversal/authz implication — paths are adapter-fixed, contents are AASM's own managed keys) and does not affect `Verdict` below; not a required GitHub check on `main` (only `CI Success` gates merge). | accepted-with-justification — follow-up filed |

> No unaddressed Critical or High finding exists in this delta.

## Trust-boundary review checklist

Not required — patch tier (trust-boundary review is a minor/major-tier
addition). No new endpoint, no loosened policy default, and no new
egress/IPC surface were introduced by the `dce039704..f6183571d` delta —
`reconcile` is an in-process Rust API parameter with no wire-protocol
exposure yet (deliberately, per `docs/src/devtools/config-ownership.md`).

## Threat-model refresh

n/a — patch tier.

## Verdict

Verdict: PASS
