# Security sign-off — v0.0.1-rc.5

- **Version:** v0.0.1-rc.5
- **Release type:** patch (same `rc` pre-release channel; counter rc.4 → rc.5)
- **Previous tag:** v0.0.1-rc.4
- **Reviewer:** Claude Code (`/release-security-gate`)
- **Date:** 2026-07-14

## Inputs reviewed

- `cargo deny check advisories` — **advisories FAILED on one `yanked` finding only** (`spin 0.9.8`, pulled in transitively via `flume` → `sqlx-sqlite` → `sqlx`). This is a **yank**, not a security advisory (no RUSTSEC vulnerability). It is **pre-existing on `master`** — `spin 0.9.8` is already pinned in the committed `Cargo.lock` and the rc.5 lockfile diff is purely the `rc.4 → rc.5` version-string churn (30/30), leaving the dependency graph unchanged. `bans`, `licenses`, and `sources` all pass. Accepted-with-justification (yank-only, no vuln, unchanged from the base).
- Open CodeQL / code-scanning alerts (agent-assembly) — inherited from rc.4 (no new code-scanning surface in this delta).
- Open Dependabot alerts (agent-assembly) — the six dependency bumps in this window are Dependabot PRs already merged to `master`; no open High/Critical alert.
- Release diff — `git log v0.0.1-rc.4..HEAD` (30 commits; 17 files, +674 / −73).
- Native `/security-review` (Anthropic diff scanner) on the release delta — **not applicable as-invoked**: the built-in scanner targets *pending working-tree changes*, and the release delta `v0.0.1-rc.4..HEAD` is fully committed (working tree clean apart from the version-bump prep on this branch). The release-diff review was therefore performed **manually** (see Findings). The delta is small and non-security-shaped: an in-binary dashboard-asset embed, a fail-loud API-key check, docs tooling (an mdBook tabs widget + tabbed installation page), a Homebrew-tap generator, and six dependency bumps.

> The release-diff review wraps the built-in `/security-review`; where the native
> scanner does not fit the committed-delta model, the equivalent manual review is
> folded into the Findings table below.

## Findings

| Severity | Finding | Status (fixed / accepted-with-justification / open) |
|---|---|---|
| Info | `cargo deny check advisories` reports one `yanked` finding (`spin 0.9.8`, transitive via `sqlx-sqlite`). Yank — not a security advisory; no RUSTSEC vulnerability. Pre-existing on `master` (already in the committed `Cargo.lock`); the rc.5 lockfile change is version-string churn only and does not alter the dependency graph. `bans` / `licenses` / `sources` all pass. | Accepted-with-justification (yank-only, unchanged from base) |
| Info | **AAASM-4517 — dashboard SPA embedded into `aa-api` (`build.rs` + `include_dir!`).** The change compiles the already-built dashboard assets into the binary and serves them when no on-disk `dashboard/dist` resolves. It adds no new network listener, no new route surface, and no new auth path — it only supplies static assets to the existing dashboard route that previously 404-ed in local mode. Static-asset embedding of first-party build output. | Reviewed — no action |
| Info | **AAASM-4572 — `aasm` validates `AASM_API_KEY` format before printing the serving banner.** This is a **tightening**: the CLI now fails loudly on a missing/invalid key instead of appearing to serve without a working credential. Regression test added for banner ordering. | Fixed (hardening) |
| Info | **AAASM-4566 / 4567 / 4573 / 4574 — mdBook tabs widget + tabbed installation docs + anchor restoration.** Hand-rolled static CSS/JS widget for the docs site and a docs-content rework; no runtime/product-code surface, no new dependency. | Reviewed — no action |
| Info | **AAASM-4520 — Homebrew tap update driven by a `metadata/versions.rb` generator.** CI/release-tooling change; generates the tap formula rather than hand-maintaining it. No product-code or trust-boundary surface. | Reviewed — no action |
| Info | Six dependency bumps in the window: `rustls` 0.23.41→0.23.42, `tokio-tungstenite` 0.29→0.30, `uuid` 1.23.4→1.23.5, `open` 5.3.6→5.4.0, `EmbarkStudios/cargo-deny-action` 2.0.20→2.1.1, `softprops/action-gh-release` 3.0.1→3.0.2. In-range/minor upgrades and SHA-pinned action bumps; `cargo deny` advisories otherwise clean (see the yank note above). | Reviewed — no action |

> No unaddressed Critical or High finding. The only `cargo deny` failure is a
> yank-only advisory on a transitive crate that is unchanged from the base tag;
> the genuinely-new surface in this window (embedded static dashboard assets, a
> fail-loud API-key check, docs tooling, a tap generator) adds no new network,
> auth, or policy surface. Consistent with the patch-tier gate (BLOCK only on
> unaddressed Critical/High).

## Threat-model refresh / trust-boundary checklist

n/a — patch tier (same `rc` pre-release channel). Net trust-boundary effect of the
delta is **neutral-to-tightening**: the dashboard-SPA embed serves first-party
static assets on an already-existing route (no new listener, route, or auth path),
and the `AASM_API_KEY` change makes the CLI fail closed on a missing/invalid key
rather than presenting a misleading serving banner. No wire-protocol change
(`protocol/v1` unchanged); no policy default loosened.

## Verdict

<!-- The token `Verdict: PASS` is what release-readiness.sh greps for. -->

Verdict: PASS
