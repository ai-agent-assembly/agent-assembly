# Security sign-off — v0.0.1-rc.4

- **Version:** v0.0.1-rc.4
- **Release type:** patch (same `rc` pre-release channel; counter rc.3 → rc.4)
- **Previous tag:** v0.0.1-rc.3
- **Reviewer:** Claude Code (`/release-security-gate`)
- **Date:** 2026-07-12

## Inputs reviewed

- `cargo deny check advisories` — **advisories ok** (exit 0; `cargo-deny` installed at `~/.cargo/bin/cargo-deny`)
- Open CodeQL / code-scanning alerts (agent-assembly) — **0 open** (API reachable; alert #4 present but **dismissed**, not open)
- Open Dependabot alerts (agent-assembly) — **0 open** (API reachable; historical alerts all in `fixed`/`dismissed` state)
- Release diff — `git log v0.0.1-rc.3..HEAD` (~370 commits; 267 files, +13,193 / −2,541)
- Native `/security-review` (Anthropic diff scanner) on the release delta — **not applicable as-invoked**: the built-in scanner targets *pending working-tree changes* on the current branch, and the release delta `v0.0.1-rc.3..HEAD` is fully committed (working tree clean). The release-diff review was therefore performed **manually** (see Findings). This mirrors the rc.3 sign-off: the delta is overwhelmingly the output of the security+QA hardening sweeps (Epics AAASM-4068 → AAASM-4333), each finding adversarially verified against real `master` before merge — that per-finding verification subsumes the diff scan for those commits. The genuinely-new attack surface in this window (the AAASM-4447 loopback gRPC listener, the Docker/CI publish paths, the two crypto/HTTP major dep bumps) was reviewed by hand.

> The release-diff review wraps the built-in `/security-review`; where the native
> scanner does not fit the committed-delta model, the equivalent manual review is
> folded into the Findings table below.

## Findings

| Severity | Finding | Status (fixed / accepted-with-justification / open) |
|---|---|---|
| Info | `cargo deny check advisories` clean (exit 0). `deny.toml` delta adds one license exception: `webpki-root-certs` (`CDLA-Permissive-2.0`) — the same Mozilla CA-bundle data license already allowed for `webpki-roots`, pulled in transitively by reqwest 0.13's rustls-platform-verifier. Not a security exception (no advisory ignored). | Reviewed — no action |
| Info | 0 open CodeQL and 0 open Dependabot alerts shipping in this release. No open High/Critical alert. | Reviewed — no action |
| Info | **AAASM-4447 / PR #1481 — local-mode gRPC `AgentLifecycleService` on `127.0.0.1:50051`.** Bind target is the hardcoded const `LOCAL_GRPC_ADDR = "127.0.0.1:50051"` (loopback), guarded by two tests: a static assertion that the shipped address is loopback / never `0.0.0.0` / port 50051, and a served-socket test proving the accepting socket is loopback and not the unspecified wildcard. The listener **reuses `aa-gateway`'s `AgentLifecycleServiceImpl` + `enrich_interceptor` + the possession-proof challenge** the remote gateway already enforces (not a re-implementation), and bounds decode at 4 MiB (parity with the gateway's legacy-grpc services). Port-in-use degrades gracefully to REST-only — it never falls back to a routable bind or to disabling auth. **Net effect: no new unauthenticated surface and no non-loopback surface added.** | Reviewed — clean, no action |
| Info | Durable local registry store (AAASM-4459): `serve_local` now backs the registry with `~/.aasm/local.db` (via `resolve_local_registry_db_path` → `GatewayConfig`), while the hermetic test constructor uses a per-pid/per-counter temp DB. API-key credential validation now indexes keys by prefix (`key_lookup`, AAASM-4075) so an invalid token no longer fans argon2 out over every key (DoS mitigation). | Fixed (hardening) |
| Info | Major dependency bumps (AAASM-4431): `ed25519-dalek` 2 → 3 (used for the IPC handshake + gateway registration signing; same `SigningKey`/`VerifyingKey`/`Signer`/`Verifier` API, no call-site change) and `reqwest` 0.12 → 0.13. Forward upgrades; `cargo deny` advisories clean. | Reviewed — no action |
| Info | CI/CD delta: new tag-only `publish-gateway` job (docker.yml) and `-p aa-api` release build (release.yml). `packages: write` is scoped to the **tag-only** publish jobs (never the PR build path); the completeness gate (release-completeness.yml) runs `permissions: contents: read` only. All actions are SHA-pinned; login uses the standard `GITHUB_TOKEN`. `aa-gateway/Dockerfile` runs on `gcr.io/distroless/static:nonroot` (non-root, minimal). No secret exposure or privilege-escalation surface. | Reviewed — no action |
| Info | Net security-relevant delta (Epics 4068–4333 sweeps): fail-closed policy hardening (tools:`"*"` wildcard now gates allow/rate-limit/approval on both paths, capability stage enforced on the primary path, empty merged cascade allow-list → Deny, unknown top-level/nested policy keys → fail-closed), budget hardening (ancestor monthly cap, unknown-model fail-closed accrual, negative-spend clamp, prompt_tokens floor), scanner detection additions (gho_/ghu_/ghr_/github_pat_/xapp-/xoxr-/ASIA prefixes, separator-stripping, base64-run floor), proxy hardening (Content-Encoding decompress-then-scan, X.509 NameConstraints on the MitM root CA, DLP on all MitM hosts, JSON-RPC batch fail-closed), dashboard token-storage → sessionStorage + CSP + RBAC gating, and poison-tolerant aa-api store locks. Every item **tightens** a control; none loosens a default. Each finding was adversarially verified against real `master` before merge. | Fixed (hardening) |

> No unaddressed Critical or High finding. Advisories are clean, there are zero
> open code-scanning / Dependabot alerts shipping in this release, and the only
> genuinely-new network surface (the AAASM-4447 loopback gRPC listener) is
> loopback-confined, decode-bounded, and reuses the gateway's already-vetted
> authenticated lifecycle service — it adds no new unauthenticated or off-host
> surface. Consistent with the patch-tier gate (BLOCK only on unaddressed
> Critical/High).

## Threat-model refresh / trust-boundary checklist

n/a — patch tier (same `rc` pre-release channel). Net trust-boundary effect of the
delta is **tightening**: policy/budget/scanner/proxy paths hardened toward
fail-closed, dashboard tokens moved to sessionStorage behind a CSP and RBAC
gating, the MitM root CA constrained with X.509 NameConstraints, and API-key
validation made argon2-DoS-resistant. The one boundary *addition* — the
AAASM-4447 local-mode gRPC `AgentLifecycleService` — is confined to loopback
(`127.0.0.1:50051`, statically + dynamically tested against a `0.0.0.0`
regression), bounds its decode at 4 MiB, and reuses the gateway's authenticated
lifecycle service (possession-proof challenge + `enrich_interceptor`) rather than
introducing a parallel unauthenticated path. No wire-protocol change
(`protocol/v1` unchanged); `TenancyMode` continues to default to the
existing-behavior fallback. No policy default loosened.

## Verdict

<!-- The token `Verdict: PASS` is what release-readiness.sh greps for. -->

Verdict: PASS
