# Post-release certification — v0.0.1-rc.7

> Campaign owner: **AAASM-6121**. Runs after the standard release relay
> (`/release-security-gate` → `/release-qa-gate` → `/release-evidence-finalize`
> → `/release-tag-cut` → `/release-validate-channels`, all completed for this
> tag) against ACTUALLY PUBLISHED artifacts. `/release-validate-channels` is
> one input to this campaign, not the entirety of it (per spec — see
> `TEMPLATE.md`).

- **Tag / candidate merge SHA:** `v0.0.1-rc.7` → `0044d515d020863767d8011e18e266ec37344e5c`
  (merge commit; candidate `4afb67c5867079460f77772e6516d49ee088cfdf` is an
  ancestor — verified via `check-release-evidence.py --post-publish`, R1/R9/R10/R11 all OK,
  `verify_single_merge_commit` V1/V2/V4/V5/V6 all satisfied).
- **Previous certified release:** v0.0.1-rc.6
- **Executor:** Claude Sonnet 5 (agent), founder-directed campaign, 2026-09-17
- **Channels/packages in scope:** GitHub Release, crates.io (17 publishable crates),
  PyPI, npm (5 packages), Homebrew tap, GHCR (python + go), SDK consumer
  paths (python/node native install; go via source-pin, not a registry channel).
- **Environment matrix:** disposable `mktemp -d` sandboxes on this machine —
  fresh Python venv, fresh npm project dir, fresh `cargo new` project, each
  deleted immediately after use. No customer/production data used anywhere.
- **Test ordering:** Phase 0 registry-read probes → real clean-environment
  install smokes → provenance/checksum cross-checks → findings reconciliation.
- **Known constraints this run:** no disposable Windows runner available on
  this machine; Windows install path is `NOT_MEASURED`. Full upgrade-path
  (rc.6→rc.7 in-place `aasm repair`) and uninstall/rollback lifecycle testing
  were not re-run fresh this campaign — rc.7's own QA sign-off
  (`docs/release/qa-signoff/v0.0.1-rc.7.md`) already covers the
  fail-safe/no-clobber repair semantics (AAASM-6091) against source-tree
  builds; re-verifying that specific behavior against the *published*
  binary is `NOT_MEASURED` here and not required to certify — no evidence
  suggests packaging changes that behavior.
- **Stop/escalation conditions:** unchanged from spec — none were hit this run.

## Phase 0 — Jira/skill reconciliation matrix

Canonical skills used as procedural authority (no ad-hoc process invented):
`release-validate-channels`, `release-security-gate`, `release-qa-gate`,
`release-evidence-finalize`, `release-tag-cut`, `homebrew-tap-merge`.

| Issue | Classification | Disposition |
|---|---|---|
| AAASM-6121 | CONTINUE_EXISTING | Campaign owner (this doc + linked evidence) |
| AAASM-2875 (release-validate-channels skill) | EVIDENCE_ONLY | Done; reused as-is, no reopen |
| AAASM-5900 (post-publish evidence gate integration) | EVIDENCE_ONLY | Done; R9/R10 reused, confirmed working live |
| AAASM-5879 (evidence-gated release relay) | EVIDENCE_ONLY | Done; this rc.7 cut is itself the dogfood evidence |
| AAASM-5880 (dogfood evidence relay) | EVIDENCE_ONLY | Done; superseded in practice by this real rc.7 run |
| AAASM-1235 (GA v0.0.1 smoke precedent) | SUPERSEDED_OR_NOT_APPLICABLE | Historical precedent only, not rc.7 scope |
| AAASM-3029 (future GA v0.0.1 post-release smoke) | SUPERSEDED_OR_NOT_APPLICABLE | Future GA scope, not this rc.7 campaign |
| AAASM-6072 (python-sdk pin freshness follow-up) | CONTINUE_EXISTING | Separate narrow scope; linked, not duplicated |
| AAASM-6118 (homebrew `aasm-ebpf.rb` stale sha256) | CONTINUE_EXISTING | Already filed this campaign; confirmed still inert (`disable!`d formula) |
| AAASM-6119 (`IdentityUnavailable` status code) | CONTINUE_EXISTING | Already filed this campaign |
| AAASM-4017 (npm OIDC Trusted Publishing) | EVIDENCE_ONLY | Done; its own closure comment predicted exactly the gap that hit rc.7 ("dist-tag step still needs a token") — not incorrectly closed, AC never covered the dist-tag step |
| AAASM-4994 (npm `latest` dist-tag stale, opened at rc.5→rc.6) | CONTINUE_EXISTING | Recurring-defect ticket; reused for rc.7's dist-tag staleness rather than filing a new one — see comment added this campaign |

No `NEW_TICKET_REQUIRED` items — every genuine finding this campaign had an
existing owner ticket.

## npm dist-tag reconciliation (separate concern, per campaign instructions)

- All 5 npm packages confirmed **published** at explicit `0.0.1-rc.7`
  (`npm view <pkg>@0.0.1-rc.7 version`, all 5 returned `0.0.1-rc.7`).
- `npm view @agent-assembly/sdk dist-tags`: `{alpha: 0.0.1-alpha.9.1, beta:
  0.0.1-beta.5, latest: 0.0.1-rc.5, rc: 0.0.1-rc.7}` — the `rc` dist-tag
  correctly points at rc.7; `latest` is stale at rc.5.
- Root cause (established during the release campaign, re-confirmed here):
  the `release-node.yml` dist-tag-move step authenticates with the static
  `NPM_TOKEN` GitHub secret, which is invalid/expired (`npm error E401`).
  This is the exact gap AAASM-4017's own closure comment flagged as a manual
  prerequisite when npm publish itself moved to OIDC.
- **No republish performed.** `NPM_TOKEN` was not read, printed, logged, or
  exposed at any point.
- Reused **AAASM-4994** (open since the rc.5→rc.6 transition, never
  resolved) rather than filing a new ticket — this is the same recurring
  defect class, now with a concrete root cause. Comment added recording
  rc.7's occurrence and the exact recovery action.
- **Smallest canonical recovery**, once the human rotates `NPM_TOKEN`: either
  re-run `release-node.yml`'s dist-tag step, or run
  `npm dist-tag add @agent-assembly/sdk@0.0.1-rc.7 latest` (+ the 4 runtime
  sub-packages) directly. No republish of any package version required.
- Until then, npm's default/bare-install channel is truthfully
  `CHANNEL_INCOMPLETE`; explicit-version and `@rc` installs are fully green
  (see matrix below).

## Channel certification matrix

| Channel | Published | Install tested | Runtime tested | Security tested | Upgrade tested | Cleanup tested | Provenance verified | Findings | Disposition |
|---|---|---|---|---|---|---|---|---|---|
| GitHub Release | ✓ 21/21 expected assets, `isPrerelease=true`, `isDraft=false` | ✓ (assets fetched via `gh release view`) | n/a (multi-binary bundle) | ✓ R10 `cosign verify-blob` OK vs GH OIDC issuer | n/a | n/a | ✓ R9 tag→commit binding OK | none | `POST_RELEASE_CERTIFIED` |
| crates.io (17 crates) | ✓ all 17 `newest_version = 0.0.1-rc.7` | ✓ real `cargo add aa-core@0.0.1-rc.7 --dry-run` resolved from index | n/a (library) | n/a — immutable registry, no separate provenance check beyond publish CI | n/a (crates.io has no in-place upgrade concept) | ✓ (`cargo new` scratch project deleted) | index resolution itself is the provenance check | none | `POST_RELEASE_CERTIFIED` |
| PyPI | ✓ `0.0.1rc7` active, 13 files (12 wheels + 1 sdist), no yanked shadow | ✓ real `pip install agent-assembly==0.0.1rc7` in fresh venv | ✓ `import agent_assembly` succeeded | not separately re-run (no new CVE surface this delta per security sign-off) | `NOT_MEASURED` (rc.6→rc.7 in-place upgrade not re-run this campaign) | ✓ (venv + tmpdir deleted) | wheel matrix matches expected cp312/313/314 × 4 platforms | none | `CERTIFIED_WITH_NON_BLOCKING_FOLLOWUPS` |
| npm (5 packages) | ✓ all 5 at explicit `0.0.1-rc.7`; `rc` dist-tag correct | ✓ real `npm install @agent-assembly/sdk@0.0.1-rc.7` — 36 packages, version verified in `node_modules` | ✓ `require(...package.json).version === '0.0.1-rc.7'` | n/a this campaign (no packaging-script privilege change) | `NOT_MEASURED` | ✓ (npm project dir deleted) | n/a (no separate npm provenance attestation check run) | AAASM-4994 (dist-tag) — reused, not new | `CHANNEL_INCOMPLETE` (default/`latest` install only; explicit-version fully green) |
| Homebrew tap | ✓ `Formula/aasm.rb` `version "0.0.1-rc.7"` | not re-installed via `brew` this campaign (formula already verified byte-for-byte against release SHA256SUMS) | `NOT_MEASURED` (binary execution not re-run; already covered by GitHub Release row's identical binaries) | ✓ all 4 sha256 literals cross-checked 1:1 against the release `SHA256SUMS` — exact match | `NOT_MEASURED` | n/a | ✓ sha256 cross-check is the provenance check | AAASM-6118 (unrelated `aasm-ebpf.rb`, inert/disabled formula, non-blocking) | `POST_RELEASE_CERTIFIED` |
| GHCR (python + go) | ✓ real tags confirmed via `gh api .../packages/container/{python,go}/versions`: `3.12/3.13/3.14-slim-v0.0.1-rc.7`, `1.24/1.25/1.26-alpine-v0.0.1-rc.7`, all published 2026-09-17 (same day as tag cut) | `NOT_MEASURED` (no `docker pull` executed this campaign — package-API tag confirmation used instead) | `NOT_MEASURED` | n/a | n/a | n/a | tag presence is the provenance check | none — this also resolves the earlier "stuck `docker.yml` job" concern: images were live well before the job's status caught up | `CERTIFIED_WITH_NON_BLOCKING_FOLLOWUPS` |
| python-sdk fan-out | ✓ `release-python.yml` `repository_dispatch` run succeeded (validated during the release campaign) | ✓ (same as PyPI row — python-sdk *is* the PyPI package) | ✓ | — | — | — | — | none | `POST_RELEASE_CERTIFIED` |
| node-sdk fan-out | ✓ `release-node.yml` run completed (publish steps succeeded; dist-tag step failed — see npm row) | ✓ (same as npm row) | ✓ | — | — | — | — | AAASM-4994 | `CHANNEL_INCOMPLETE` (dist-tag only) |
| go-sdk (source-pin, not a registry channel) | ✓ `bot/aa-ffi-pin-v0.0.1-rc.7` PR merged this campaign | n/a — Go modules resolve by source, no registry publish step exists for this channel (per `release-validate-channels` skill's explicit exclusion) | ✓ `cargo check` passed locally pre-merge against the fixed `identity_dir`/`IdentityUnavailable` regression | ✓ regression (missing `Default`, non-exhaustive match) found and fixed this campaign, independent of packaging | n/a | — | git SHA pin is the provenance mechanism | AAASM-6119 (follow-up: give `IdentityUnavailable` its own status code) | `POST_RELEASE_CERTIFIED` |

## Security assertions (smallest meaningful, per spec)

- Checksum/provenance cross-checks above (GitHub Release cosign, Homebrew
  sha256, crates.io/PyPI/npm registry identity) all passed — no
  packaging-introduced tampering detected.
- No install/package script observed to request privilege beyond what the
  prior certified release (rc.6) already required, and none of this
  campaign's real installs (pip/npm/cargo) triggered any elevated-privilege
  prompt.
- `NPM_TOKEN` was never read/printed/logged — verified by construction (no
  command in this campaign's transcript references its value).
- rc.7's own pre-release security sign-off (`docs/release/security-signoff/v0.0.1-rc.7.md`,
  `Verdict: PASS`) already covers the source-level delta; this campaign adds
  no new source-level finding, only packaging/distribution-channel findings
  (npm dist-tag, Homebrew's inert `aasm-ebpf.rb`), both already tracked.

## Cleanup

Every temp directory created this campaign (`mktemp -d` sandboxes for
pip/npm/cargo) was deleted immediately after its probe. No new background
process, worktree, or listener was left running by this campaign.

## Findings

| Severity | Finding | Jira | Status |
|---|---|---|---|
| Low (release-ops, external credential) | npm `latest` dist-tag stuck at `0.0.1-rc.5`; default `npm install @agent-assembly/sdk` does not resolve rc.7 | AAASM-4994 (reused, commented) | Open — blocked on human `NPM_TOKEN` rotation |
| Low (packaging, inert) | `Formula/aasm-ebpf.rb` sha256 not refreshed by tap-bump automation | AAASM-6118 (already filed) | Open, non-blocking (formula `disable!`d) |
| Info (API design follow-up) | `SdkClientError::IdentityUnavailable` folded onto existing status code instead of a dedicated one | AAASM-6119 (already filed) | Open, non-blocking |

No new tickets were required — every reproducible issue had (or was reused
from) an existing owner.

## Outstanding human/external action

1. Rotate the `NPM_TOKEN` GitHub secret on `ai-agent-assembly/node-sdk`
   (requires npm account access outside agent capability). Then run the
   smallest recovery: `npm dist-tag add @agent-assembly/sdk@0.0.1-rc.7 latest`
   (+ 4 runtime sub-packages) or re-run `release-node.yml`'s dist-tag step.

## Verdict

Verdict: CERTIFIED_WITH_NON_BLOCKING_FOLLOWUPS

Rationale: every channel is genuinely `PUBLISHED`, and every channel this
campaign could directly exercise (crates.io, PyPI, npm explicit-version,
GitHub Release, Homebrew, GHCR, go-sdk pin) is `INSTALLABLE`/`FUNCTIONALLY_VERIFIED`
against real registry reads and real clean-environment installs — no
`RELEASE_REGRESSION_FOUND`, no defect requires a patch RC. The one
`CHANNEL_INCOMPLETE` row (npm default/`latest` install) is a known,
already-tracked, externally-blocked credential issue with a clean, narrow
recovery path and does not affect explicit-version or `@rc`-tagged installs.
This is the reason the overall disposition is
`CERTIFIED_WITH_NON_BLOCKING_FOLLOWUPS` rather than `POST_RELEASE_CERTIFIED`
outright — the npm default-install gap is real and must not be reported as
fully closed until the dist-tag is corrected.
