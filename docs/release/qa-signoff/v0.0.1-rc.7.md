# QA sign-off — v0.0.1-rc.7

> **RE-VERIFICATION RUN (2026-08-23, independent QA/security session).**
> Anchored on `9c17c594a2d72010fb819c8f46484ca9da879199` — 5 commits past this
> file's original `c4686f317b` baseline: the AAASM-5832 and AAASM-5833 fixes
> (both `Done` in Jira, both merged via PR #2141 / #2139 with 0 non-success
> CI checks), plus AAASM-5834's unrelated `aa-ebpf` cross-compile fix (#2140).
> Findings F-1/F-2 below were independently re-reproduced against a locally
> built `aasm` release binary at this SHA — see "Re-verification" section.
> The rest of this document (baseline discovery, risk map, journey PASS
> rows) is the original real post-merge dogfood run and was not re-executed;
> its shape was confirmed unchanged by re-running the manifest/risk-mapper
> scripts at the new SHA (identical `overall_risk: HIGH`, identical P0 set).
>
> Original header follows, unmodified:
>
> **REAL POST-MERGE RUN.** This replaces the earlier pre-merge dry-run version
> of this file. All 12 AAASM-5819 child PRs (#2119-#2130) are merged to `main`
> via merge commit (admin-authority merge per explicit session policy —
> self-approval is blocked by GitHub for the PR author; independent
> self-review + LGTM comment recorded on each PR instead). This is a real
> dogfood of `/release-qa-gate` against the actual merged base, exercising
> live infrastructure this session stood up itself (gateway, API server,
> published Homebrew binary, published GHCR container, published PyPI/npm/Go
> packages) — not a candidate-delta simulation.

- **Version:** v0.0.1-rc.7 (label; no tag cut by this run — tagging is out of
  scope per Release Operations Policy, this session is observational)
- **Release type / depth:** patch (QA-infrastructure delta: Markdown/YAML/
  shell/Python; no `aa-*` crate source changed by AAASM-5819 itself)
- **Previous tag / prior verified baseline:** v0.0.1-rc.6 (`8ab5ee8e479aa03c16f27c28fdf16f0d8a9d4804`)
- **Reviewer / executor:** Claude Code session, AAASM-5819 implementation campaign
- **Date:** 2026-08-23

## Baseline

- **Repository:** ai-agent-assembly/agent-assembly
- **Base branch:** main
- **Verified HEAD SHA (real, canonical `remote/main`, all 12 PRs merged):** `c4686f317bd2a8dbe8006b0621e6dd8d04f41306`
- **Previous trusted QA baseline:** none — first `/release-qa-gate` run
- **Verification manifest:** regenerated for real at this SHA via
  `scripts/qa/build-verification-manifest.sh` — `baseline.source: "unknown"`
  (correct, no prior sign-off), `head_sha` correctly resolved to `c4686f317b`.
- **Risk map:** regenerated for real via `scripts/qa/map-risk.py` against the
  full `v0.0.1-rc.6..HEAD` delta (2444 files, +353288/-17697) —
  `overall_risk: HIGH`, `lanes: [design, docs, functional, reliability,
  security]`, `fallback_used: true` (conservative MEDIUM fallback on
  unmatched paths, not a silent drop), 33 journeys selected — identical shape
  to the pre-merge dry run's own risk classification, confirming the risk
  mapper is deterministic across the merge.

## Risk classification

- **Overall release risk tier:** HIGH
- **HIGH-risk surfaces touched this release:** `scripts/release-readiness.sh`
  (release/supply-chain rule), plus the union of surfaces touched across all
  12 child PRs (QA gate skill, risk mapper, manifest generator, readiness
  gate, evidence contract, Jira dependency graph tooling)
- **Deep-sweep triggered?** no — patch-tier delta, no major/trust-boundary
  change, cadence not due

## Selected journeys

Full P0 set (mandatory):

| Journey ID | Priority | Result | Evidence |
|---|---|---|---|
| J04 | P0 | **PASS** | Real Homebrew install: `brew install ai-agent-assembly/tap/aasm` (already current at 0.0.1-rc.6); `/opt/homebrew/bin/aasm --version` → `aasm 0.0.1-rc.6`. Published-artifact path, not source build. |
| J08 | P0 | **PASS** | Live `aa-gateway` + `aa-api-server` stood up locally (127.0.0.1:50351 / :8180); real Python SDK (`agent-assembly==0.0.1rc6` from PyPI) registered a live agent against the gateway — `ctx.registered == True`, agent visible in `aasm agent list` (real REST call, real table output). |
| J17 | P0 | **PASS** | Real pull of the published gateway artifact: `docker pull ghcr.io/ai-agent-assembly/aa-gateway:latest` succeeded; ran it (`docker run ... --policy /policy.yaml --listen 0.0.0.0:50051`), confirmed real startup log sequence (policy load, audit writer, gRPC server bound) and host-side TCP reachability on the mapped port. Published-artifact path, not source build. |
| J19 | P0 | **PASS** | Live gateway + `aa-api-server`: `aasm policy apply policy-examples/low-risk.yaml` → real "Policy applied successfully" with version/timestamp; `aasm policy get` immediately after returned the exact applied YAML content back — genuine round-trip, not just exit-0. `aasm policy validate` also exercised on 2 distinct real policy files (low-risk.yaml clean, strict.yaml with a real warning on an unenforced capability). |
| J21 | P0 | **PASS** | `mdbook build` of `docs/` succeeded; every documented command in `docs/src/quick-start/*.md` (requirements/installation/configuration/first-run) executed for real and matched documented output; 10-command spot-check of `docs/src/cli/*.md` flags/subcommands against real `--help` output — no discrepancies. |
| J24 | P0 | **PASS** | Live gateway with `policy-examples/low-risk.yaml` loaded at startup, confirmed via real startup log (`loading policy`) and `aasm gateway status` cross-checked against `lsof`/PID. |
| J41 | P0 | ~~UNTESTED_OR_BLOCKED~~ → **PASS (re-verified)** | Original basis (struck through): SDK+proxy+eBPF exercised together not attempted — macOS dogfood host, eBPF Linux-only. **Re-verification (2026-08-23):** `e2e — eBPF (Linux)` CI job's "Run AAASM-1523 e2e three-layer suite" step — the exact SDK/proxy/eBPF combined-audit-stream acceptance contract for this journey — ran on Linux CI for PR #2140 (part of the tested SHA's ancestry) and returned SUCCESS. This was a session-scoped execution gap (no Linux runner used), not a true product gap: Linux CI has covered this journey on every eBPF-touching PR all along. |
| J53 | P0 | **PASS** | Real pull + real run of `ghcr.io/ai-agent-assembly/aa-gateway:latest` (see J17 — same container, this journey's own ID). Container reached a healthy listening state, confirmed via `docker logs` and a real TCP connect from the host. Torn down after. |
| J56 | P0 | **PARTIAL** | Real published-package Python SDK smoke (PyPI `agent-assembly==0.0.1rc6`) against a live gateway succeeded (registration). The full "clean-env -> governed-Python-agent" flow (a real framework tool call intercepted and a policy decision recorded) was not completed — `AssemblyContext` in this SDK version exposes registration/adapters but no direct decision-check API was exercised; would need a real LangChain/similar wrapped call, not attempted this run. |
| J59 | P0 | ~~PARTIAL~~ → **PASS (re-verified)** | Deploy/observe/govern legs PASS (unchanged). Original recover leg (struck through): **2 real defects found here** (AAASM-5832 / AAASM-5833). **Re-verification (2026-08-23):** both independently reproduced fixed against a locally built release `aasm` binary at `9c17c594a`. F-1: held port 18099 with a controlled Python listener (confirmed via `lsof` before launch), `aasm gateway start` correctly reported `error: aa-gateway (pid …) exited before becoming ready — it did not bind 127.0.0.1:18099`, exit code 1 (previously: false "started" + live PID). F-2: started a real gateway, `kill -9`'d its PID, confirmed via `ps -p` the process was gone, then `aasm gateway status` correctly reported `Gateway: not running (stale PID file — process … is no longer alive)`, exit code 1 (previously: false "pid exists but port is unreachable"). Recover leg now tells the truth; journey PASS. |

## Efficiency evidence

Per AAASM-5831's AC — a compact comparison against the old giant-prompt/full-
sweep approach, not precise token accounting:

- **Shared discovery performed once, reused by every worker.** The
  verification manifest and risk map were each generated exactly once at the
  merged HEAD and handed to all 3 parallel dogfood workers as input — no
  worker re-derived the delta, re-ran `git diff`, or re-classified risk. The
  old approach (one giant prompt driving the whole dogfood serially) would
  have re-established this context on every lane switch.
- **Risk-selected scope, not a blind full sweep.** 33 of the catalog's 60
  total journeys were selected (10/10 P0 + 17 of 33 P1s + 6 of 17 P2s) —
  driven by the risk mapper's `HIGH` classification and `fallback_used` flag,
  not by running all 60 unconditionally.
- **Deliberately skipped, not silently dropped:** the Design/dashboard lane
  (no Playwright session stood up — recorded as a real coverage gap, not
  treated as passing) and J41 (eBPF — Linux-only, this host is macOS,
  recorded as a genuine platform gap). Both are named in the Skipped/blocked
  table above rather than omitted.
- **3 of 5 available worker slots used**, 2 reserved — consistent with the
  policy's "a narrow patch scope should use 1-3" guidance and the requirement
  to reserve capacity for finding-verification rather than filling all 5
  slots by default.
- **Dedup performed once per candidate finding, not re-litigated.** Both
  AAASM-5832 and AAASM-5833 got exactly one Jira dedup search each before
  independent verification, not a repeated re-search per verification pass.

## Lane results

| Lane | Result | Evidence reference |
|---|---|---|
| Functional / config | PASS | Gateway lifecycle, CLI command matrix, policy apply/get/validate round-trips — all real, against a live gateway + live API server this session stood up |
| Golden journeys | **Re-verified: PASS (9/10 P0), PARTIAL (1/10: J56)** — was PASS (7/10 P0), PARTIAL (2/10: J56, J59), UNTESTED_OR_BLOCKED (1/10: J41) | See journey table |
| Design | UNTESTED | No dashboard/browser (Playwright) session stood up this run — scope gap, not attempted |
| Reliability | PARTIAL — **2 findings** | Malformed-policy fail-closed VERIFIED correct (gateway refuses to load, exits, `aasm gateway status` correctly reports not-running); port-collision false-success (AAASM-5832) and post-death false-liveness (AAASM-5833) both independently reproduced twice each |
| Docs / product consistency | PASS | Full quick-start command sequence + 10-command CLI-flag spot-check, all real, all matching |
| Security-relevant behavior | UNTESTED (independent) | Out of scope for this QA dogfood — `/release-security-gate` is the independent authority; confirmed **zero diff** on `.claude/skills/release-security-gate/` across the entire `v0.0.1-rc.6..HEAD` range — this Epic did not touch or weaken it |

## Skipped / blocked / untested coverage

| Item | Reason | Risk if unresolved |
|---|---|---|
| ~~J41 (SDK+proxy+eBPF combined)~~ | Resolved 2026-08-23 by Linux CI evidence (`e2e — eBPF (Linux)` / AAASM-1523 suite, PR #2140) — see journey table | — |
| J56 full completion (real intercepted tool call -> policy decision -> audit entry) | This SDK version's `AssemblyContext` exposes no direct decision-check API in the smoke path attempted; a real framework-wrapped tool call was not exercised | MEDIUM — the registration/config path is verified, the actual enforcement-decision path along this exact journey is not |
| Design lane (dashboard/Playwright) | Not stood up this run | MEDIUM — no browser-based QA coverage this run |
| Audit trail for administrative actions (policy apply) | `aasm audit list` returned no entries after a real `policy apply` and a real SDK agent registration — investigated; `audit list`'s filters (`ToolCallIntercepted`, `PolicyViolation`) suggest audit is scoped to runtime policy *decisions*, not admin/config actions or registration events, which would make this expected behavior rather than a defect — **not confirmed either way this run**, flagging as an open question rather than a finding (no clear expected-behavior contradiction to file a Bug against) | LOW-MEDIUM — needs a follow-up run exercising a real tool-call decision end-to-end to know if the audit gap is real |

## Findings

| ID | Severity | Finding | Jira | Status |
|---|---|---|---|---|
| F-1 | Medium | `aasm gateway start` reports success + a live PID when the spawned gateway actually failed to bind (AddrInUse) — readiness probe checks "is anything listening", not "is my spawned child the listener" | AAASM-5832 | **FIXED, re-verified 2026-08-23** — PR #2141 (merged, 0 non-success CI checks); behaviorally reproduced-as-fixed against a locally built binary (see J59 re-verification evidence above) |
| F-2 | Medium | `aasm gateway status` claims a dead PID "exists" after the gateway process has actually died — contradicts a direct `kill -0` check and contradicts `aasm gateway stop`'s own (correct) liveness check on the same PID | AAASM-5833 | **FIXED, re-verified 2026-08-23** — PR #2139 (merged, 0 non-success CI checks); behaviorally reproduced-as-fixed against a locally built binary (see J59 re-verification evidence above) |

Both independently reproduced (2 separate repro methods each) before filing, per the AAASM-5827 finding-verification protocol. Both are now fixed and independently re-verified against a real built binary in this re-verification run — not inferred from the diff or CI alone.

## Known non-blocking issues / residual risk

- SDK worker flagged a minor error-message clarity issue in the Node SDK's
  `auto` registration mode (misleading "already registered" text when no
  sidecar is actually reachable) — not filed, non-blocking, cosmetic.
- CLI worker flagged that `aasm status`/`agent list`/`policy` commands talk to
  a separate REST layer (`aa-api-server`, default :8080) from what
  `aasm gateway start` stands up (`aa-gateway` gRPC), with no CLI-level
  indication of the two-tier split — likely working-as-designed per the
  documented architecture, flagged for product-owner UX judgment, not filed.

## Waivers

None sought or granted. F-1/F-2 no longer need one (fixed, re-verified).
**J56's Medium-on-P0 coverage gap (SDK enforcement-decision path not
exercised) remains open and unwaived** — per the release QA policy ("Medium
severity findings on P0 or otherwise-critical paths require either a fix +
reverification, or an explicit human waiver... Absent explicit waiver,
unresolved Medium-on-P0 ... forces BLOCK"), this alone is sufficient to keep
the verdict at BLOCK. No dup Jira ticket found for this specific gap (search:
"decision-check", "AssemblyContext", "enforcement-decision" — no match); not
filed as a Bug because it is an unconfirmed coverage gap (possibly
by-design SDK API surface), not a reproduced defect, per the finding
protocol.

## Verdict

<!-- The token `Verdict: PASS` is what release-readiness.sh greps for.

     RE-VERIFICATION UPDATE (2026-08-23): this run independently re-verified
     the two original BLOCK reasons (F-1/AAASM-5832, F-2/AAASM-5833) as
     FIXED — both behaviorally reproduced-as-fixed against a locally built
     release binary at 9c17c594a, not just inferred from the diff or from
     merged-PR CI. It also found that J41 (the third original BLOCK
     contributor) was never actually a product gap: Linux CI has run its
     exact acceptance contract (AAASM-1523 e2e three-layer suite) on every
     eBPF-touching PR, including one in this SHA's ancestry (#2140,
     SUCCESS) — the prior UNTESTED_OR_BLOCKED classification was an
     artifact of that session's macOS-only local dogfood, not a real gap.

     This run remains Verdict: BLOCK — but for one narrower, pre-existing
     reason only: J56's Medium-severity, P0-path coverage gap (the SDK
     version under test exposes no direct decision-check API in the smoke
     path attempted; a real framework-wrapped tool call intercepted through
     to a policy decision was never exercised) is still open and unwaived.
     This was already true in the original BLOCK sign-off and was out of
     scope for this session's AAASM-5832/5833-focused re-verification to
     close — completing it needs either a real framework-integration QA
     pass (stand up a live gateway + SDK + a real wrapped tool call, e.g.
     via python-sdk's documented adapter path) or an explicit engineer
     waiver recorded here. Do not read this BLOCK as unresolved regression
     risk from the AAASM-5832/5833 fixes themselves — those are clean. -->

Verdict: BLOCK
