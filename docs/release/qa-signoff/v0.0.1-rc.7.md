# QA sign-off — v0.0.1-rc.7

- **Version:** v0.0.1-rc.7
- **Release type / depth:** patch (rc.6 → rc.7, same channel) — risk-mapper elevates depth to HIGH due to `aa-runtime/src` in the delta
- **Previous tag / prior verified baseline:** v0.0.1-rc.6 (prior rc.7 sign-off candidate: `dce0397048eae5cf8465fc737eb9db9af78614d6`)
- **Reviewer / executor:** Claude Sonnet 5 (agent), founder-directed rc.7 re-validation for AAASM-6091
- **Date:** 2026-09-13

## Why this sign-off was regenerated

Founder decision for AAASM-6091 changed `Engine::repair()`'s mutation
semantics. The founder explicitly required a **fresh** re-validation — not
reuse of the prior rc.7 QA sign-off (candidate `dce039704`) — and a
**genuinely fresh, real-device J79 exercise against this exact candidate
lineage**, explicitly forbidding reuse of any prior J79 pass. See J79's row
below.

This is also the first rewrite of this file's "Selected journeys" table
into the machine-parseable one-journey-per-row format
`scripts/qa/build-release-evidence.py` requires (`^J\d+[A-Za-z]?$` per row)
— an earlier draft grouped several journey IDs per row for readability,
which the parser silently could not read, defaulting every grouped journey
to `NOT_RUN` in the generated evidence JSON and blocking
`check-release-evidence.py`'s R1 rule. Corrected here; see
`docs/release/qa-signoff/v0.0.1-rc.7.evidence.json` (regenerated fresh
against this exact table).

## Baseline

- **Repository:** ai-agent-assembly/agent-assembly
- **Base branch:** main
- **Verified HEAD SHA:** `09a8d97638df8de80f2aa8ce5ddeb8a26f523ca6`
- **Previous trusted QA baseline:** `dce0397048eae5cf8465fc737eb9db9af78614d6`
- **Candidate advanced since this file's original AAASM-6091 write (`f6183571d` → `09a8d97638`):** a real AAASM-6091 test-coverage gap fixed (`aa-cli/tests/integrations_claude_code.rs`, test-only, independently reviewed, PR #2438); the real-device J79 evidence + registry update (PR #2440); this file's own table-format correction (PR #2442); and one unrelated concurrent merge (PR #2439, `.claude/skills/*` and `.codex/skills/*` tooling/skill config only — no product code, no `Cargo.toml`/`.rs` file, confirmed via `gh pr view 2439 --json files`). No product-code path changed beyond the test file already named.
- **Verification manifest:** `.qa/verification-manifest.json` (generated fresh against this exact HEAD)
- **Delta since previous baseline (`dce039704`..`f6183571d`, 24 commits, 28 files):** AAASM-6091 — the fail-safe/no-clobber `Engine::repair()` fix (PR #2436) and its independent-adversarial-review follow-up (PR #2437).
- **Fresh functional evidence:** CI's own `Test` job for the AAASM-6091 candidate lineage — `f6183571d`'s `Test` job: `https://github.com/ai-agent-assembly/agent-assembly/actions/runs/34714811739/job/103610164591`, **8254/8254 tests passed**, 24 skipped (documented), 0 failed. The test-coverage-gap fix (`cf34aceda`, merged via PR #2438 into `4d9c29959`) has its own fresh `Test: success` on that merge commit (verified live via `gh api .../commits/4d9c29959.../check-runs`). A local `cargo nextest run -p aa-core -p aa-runtime -p aa-cli -p aa-devtool-{windsurf,claude-code,codex,copilot} -p aa-integration-tests --test '*'` (broader than CI's default invocation) surfaced the gap itself — two tests in `aa-cli/tests/integrations_claude_code.rs` (`the_command_family_drives_the_native_claude_code_integration`, `a_restarted_service_answers_the_callers_project_and_refuses_a_strangers`) still assumed `repair` unconditionally restores an externally-changed AASM-owned key, missed by PR #2436's original test-fixing sweep (which covered `integrations_command.rs`, `integrations_outcome_contract.rs`, `integrations_repair_no_install.rs`, but not this file). Fixed and reverified individually and as a full-file re-run (11/11 pass).
- Independent adversarial review of the AAASM-6091 diff itself (general-purpose read-only agent, not self-graded): 2 major + 2 minor findings, all fixed in PR #2437 (credential-withholding test added, doc guarantee correctly scoped, `Engine::apply()`'s pre-existing gap disclosed and filed separately as AAASM-6092, two dropped Known Gaps items restored).
- `cargo deny check advisories`: clean. Dependabot alerts: 0 open. CodeQL alerts: 0 open. (re-checked fresh against `09a8d97638`, see security sign-off).

## Risk classification

- **Overall release risk tier:** HIGH (per `scripts/qa/map-risk.py` — `aa-runtime/src` touched, a HIGH-risk surface)
- **HIGH-risk surfaces touched this release:** `aa-runtime/src/devint/*` (DI-API `reconcile` wiring — in-process only, not wire-exposed yet)
- **Deep-sweep triggered?** no — patch tier; HIGH classification here is driven entirely by `aa-runtime` being touched at all, not by breadth of change

## Selected journeys

| Journey ID | Priority | Result | Evidence |
|---|---|---|---|
| J04 | P0 | **UNTESTED_OR_BLOCKED** (waived — AAASM-6067-waiver-published-artifact) | Unaffected by the AAASM-6091 delta; waiver from the prior rc.7 campaign carries forward unchanged — see Waivers. |
| J08 | P0 | **PASS** | Python SDK worker: direct live execution against source, not published-artifact dependent; unaffected by this delta. |
| J17 | P0 | **UNTESTED_OR_BLOCKED** (waived — AAASM-6067-waiver-published-artifact) | Unaffected by the AAASM-6091 delta; waiver carries forward unchanged — see Waivers. |
| J19 | P0 | **PASS** | Policy-core worker: live-process test execution; unaffected by this delta. |
| J21 | P0 | **PASS** | Docs/reliability worker; unaffected by this delta. |
| J24 | P0 | **PASS** | Policy-core worker: live-process test execution; unaffected by this delta. |
| J27 | P0 | **PASS** | Policy-core worker: live-process test execution; unaffected by this delta. |
| J41 | P0 | **PASS** | `aa-runtime/src` HIGH-risk surface — touched code is `aa-runtime/src/devint/{lifecycle,service,server,testkit}.rs` (threading the new `reconcile` param). CI's `Test` job covers `aa-runtime`'s full suite on the candidate lineage. Independent adversarial review confirmed `reconcile` cannot broaden scope beyond already-in-scope drift findings and is not wire-exposed. |
| J53 | P0 | **UNTESTED_OR_BLOCKED** (waived — AAASM-6067-waiver-published-artifact) | `qa/golden-journeys.yaml`'s "Pull, configure & operate the aa-gateway container image" journey — unaffected by the AAASM-6091 delta; waiver carries forward unchanged — see Waivers. |
| J56 | P1 | **PASS** | Unaffected by this delta; prior rc.7 sign-off's evidence stands. |
| J59 | P0 | **UNTESTED_OR_BLOCKED** (waived — AAASM-6067-waiver-published-artifact) | Unaffected by the AAASM-6091 delta; waiver carries forward unchanged — see Waivers. |
| J62 | P0 | **PASS** | Unaffected by this delta; prior rc.7 sign-off's evidence stands. |
| J65 | P0 | **PASS** | Unaffected by this delta; prior rc.7 sign-off's evidence stands. |
| J66 | P0 | **PASS** | Unaffected by this delta; prior rc.7 sign-off's evidence stands. |
| J70 | P0 | **PASS** | Unaffected by this delta; prior rc.7 sign-off's evidence stands, and real-hosted install/verify/remove exercised again live as part of this campaign's J79 work (see J79 below), the strongest form of this journey's evidence. |
| J79 | P1 | **PASS** | Founder-authorized real macOS administrator install/verify/rollback performed against candidate `4d9c29959` (post-AAASM-6091; no product-code change since to `09a8d97638`), a genuinely fresh exercise, not the prior candidate's (`dce039704`) reused claim. `scripts/measure-claude-code-managed-enforcement.sh` refused pre-install (exit 5, clean step-0). Real `MacOsAdminAuthority` write: file installed root:wheel, mode 644, SHA matches the disclosed plan exactly; direct-rewrite and directory-write attempts refused by the OS. `aasm integrations verify` progressed truthfully from `partially_passed` (proxy not started) to `passed` (all 4 assertions hold, including real synthetic-credential redaction) once the proxy was started. Real rollback confirmed the host restored to exact step-0 state. Full evidence: `verification-reports/AAASM-5308-managed-enforcement-evidence-2026-09-13.md`; `qa/golden-journeys.yaml`'s J79 entry updated `lifecycle_state: gap → manual_live` accordingly. Two items explicitly NOT MEASURED, not waived (per-key override resistance — no standard non-admin account on this host; server-managed-settings/`forceRemoteSettingsRefresh` interaction — needs an interactive session): AAASM-5276 condition C6's runtime-precedence half stays open on AAASM-5308 for this reason, but this does not affect J79's own outcome bar (the real admin-authority write/remove path itself), which is fully satisfied. |
| J64 | P1 | **PASS** | `lifecycle_state: manual_live`, real Apple-Silicon hardware qualification. AAASM-6073 (the prior campaign's staleness finding) is **Resolved**: a fresh, genuine hardware re-qualification was performed against candidate `2b8af389623acd0a02107932a4b9a46bd0b94721` (PR #2416, merged `9db9d05fc3fab9bcd7d4909bc65d4eb526a2203e`) — real Landlock-capable kernel, real cross-compiled guest binaries, real ext4 rootfs, real codesigned Virtualization.framework helper, 10/10 real-hardware tests pass. `git diff --name-only 9db9d05fc..09a8d97638 -- aa-isolation-macos-vm aa-isolation-macos-vm-poc` is empty — no isolation-path file has changed since re-qualification, so the qualified evidence bridges cleanly to this candidate. Full evidence: `verification-reports/verification-report-AAASM-5811-hardware-qualification.md` (superseded/updated in place by PR #2416). |
| J67 | P1 | **PASS** | `lifecycle_state: automated`. Cited tests (`aa-cli::integrations_stalled_runtime`, `aa-cli::integrations_provenance`) confirmed PASS on candidate `4d9c29959`'s `Test` job (https://github.com/ai-agent-assembly/agent-assembly/actions/runs/34734773674/job/103664201625) — this candidate is the AAASM-6091 lineage commit that itself touched `aa-cli/tests/integrations_claude_code.rs`/`integrations_command.rs`/`integrations_repair_no_install.rs` and `aa-core/src/integration/*`, so this run is fresh evidence against the actual delta, not a stale bridge from before it. `git diff --name-only 4d9c29959..09a8d97638 -- '*.rs' Cargo.toml Cargo.lock` is empty, so this evidence bridges cleanly to the final candidate. |
| J68 | P1 | **PASS** | `lifecycle_state: automated`. Cited tests (`aa-cli::integrations_command`, `aa-cli::integrations_claude_code`) confirmed PASS on the same `4d9c29959` `Test` job as J67 — the same run that directly includes the AAASM-6091 test-coverage-gap fix to this exact file. |
| J69 | P1 | **PASS** | `lifecycle_state: automated`. Cited tests (`aa-cli::integrations_claude_code`, 5 selectors) confirmed PASS on the same `4d9c29959` `Test` job as J67. |
| J71 | P1 | **PASS** | `lifecycle_state: automated`. Cited `aa-integration-tests::conformance_claude_code` tests confirmed PASS on the same `4d9c29959` `Test` job as J67. |
| J72 | P1 | **PASS** | `lifecycle_state: automated`. Cited `aa-integration-tests::conformance_claude_code` tests confirmed PASS on the same `4d9c29959` `Test` job as J67. |
| J73 | P1 | **PASS** | `lifecycle_state: automated`. Cited `aa-integration-tests::conformance_claude_code` + `aa-cli::integrations_command` tests confirmed PASS on the same `4d9c29959` `Test` job as J67. |
| J74 | P1 | **PASS** | `lifecycle_state: automated`. Cited `aa-integration-tests::conformance_claude_code` + `aa-cli::integrations_remove_no_install` tests confirmed PASS on the same `4d9c29959` `Test` job as J67. |
| J75 | P1 | **PASS** | `lifecycle_state: automated`. Cited tests across `integrations_command`/`integrations_claude_code`/`integrations_repair_no_install`/`integrations_remove_no_install`/`integrations_provenance`/`conformance_claude_code` confirmed PASS on the same `4d9c29959` `Test` job as J67. |
| J76 | P1 | **PASS** | `lifecycle_state: automated`. Cited in-source `aa-runtime::devint::threat_model` test confirmed PASS on the same `4d9c29959` `Test` job as J67 — the same run covering `aa-runtime/src/devint/{lifecycle,server,service,testkit}.rs`, the crate this release's HIGH-risk classification is scoped to. |
| J77 | P1 | **PASS** | `lifecycle_state: automated`. Cited tests across `integrations_command`/`aa-devtool-contract::lifecycle_contract`/`aa-core::integration::contract` confirmed PASS on the same `4d9c29959` `Test` job as J67. |
| J78 | P1 | **PASS** | `lifecycle_state: automated`. Cited tests across `integrations_claude_code`/`claude_code_integration_lifecycle`/`aa-devtool-claude-code::managed_settings` confirmed PASS on the same `4d9c29959` `Test` job as J67. |
| J80 | P1 | **PASS** | `lifecycle_state: automated`. Cited `aa-cli::proxy_build_identity_probe` tests confirmed PASS on the same `4d9c29959` `Test` job as J67. |

**J64/J67–J80 (14 release_blocking registry entries, backfilled into `qa/golden-journeys.yaml` by AAASM-5999 after this release's original risk-mapped journey selection ran):** all 14 are now genuinely closed for this candidate — a real change from the prior rc.7 campaign, where J64 was an open, explicitly-not-waived FAIL. J64's staleness (AAASM-6073) was resolved by a fresh real-hardware qualification (PR #2416) before this AAASM-6091 re-validation began, and the AAASM-6091 delta itself did not touch the isolation path. The 13 `automated` journeys are covered by the same `4d9c29959` CI `Test` job that already exercises the exact files AAASM-6091 changed (`integrations_claude_code.rs` most directly), which is a stronger bridge than the prior campaign's — that one cited a Test run that predated the file it was vouching for.

`aa-devtool-windsurf`, `aa-devtool-claude-code`, `aa-devtool-codex`, and
`aa-devtool-copilot` were all touched by the AAASM-6091 delta
(fail-closed-on-malformed-config fixes) but no golden journey covers this
exact scenario (J63 and J77 are adjacent but test different things —
`aasm run` launch conformance and capability-declaration conformance,
respectively). CI's `Test` job covers all four crates' unit/integration
suites on the candidate lineage; independent review confirmed the
fail-closed changes are hardening, not regressions. Not a table row because
no journey ID applies.

## Waivers

**Granted.** Bryant Liu (verified `ai-agent-assembly` GitHub org owner,
`Chisanan232`), release owner, explicitly approved the narrow waiver below
during the AAASM-6067 campaign, 2026-09-08 — carried forward unchanged into
this AAASM-6091 re-validation since no published-artifact-path code changed
in the intervening delta. Ref for all entries below:
`AAASM-6067-waiver-published-artifact`.

- **Waived by:** Bryant Liu (verified org owner `Chisanan232`), 2026-09-08, via chat approval in the AAASM-6067 release-readiness campaign
- **Condition waived:** J04 `UNTESTED_OR_BLOCKED` (ref: `AAASM-6067-waiver-published-artifact`) — published-artifact fidelity requirement, not verifiable pre-tag
- **Justification:** Structural sequencing gap common to every release at this exact point (bump → tag → publish); underlying CLI install capability independently verified via source review on the candidate SHA.
- **Post-tag obligation:** MUST be re-verified against the actual published rc.7 artifact after tagging, before this waiver is considered discharged.

- **Waived by:** Bryant Liu (verified org owner `Chisanan232`), 2026-09-08, via chat approval in the AAASM-6067 release-readiness campaign
- **Condition waived:** J17 `UNTESTED_OR_BLOCKED` (ref: `AAASM-6067-waiver-published-artifact`) — published-artifact fidelity requirement, not verifiable pre-tag
- **Justification:** Structural sequencing gap common to every release at this exact point; underlying gateway install capability independently verified via source review on the candidate SHA.
- **Post-tag obligation:** MUST be re-verified against the actual published rc.7 artifact after tagging.

- **Waived by:** Bryant Liu (verified org owner `Chisanan232`), 2026-09-08, via chat approval in the AAASM-6067 release-readiness campaign
- **Condition waived:** J53 `UNTESTED_OR_BLOCKED` (ref: `AAASM-6067-waiver-published-artifact`) — published-artifact fidelity requirement, not verifiable pre-tag
- **Justification:** Structural sequencing gap common to every release at this exact point; underlying gateway container capability independently verified via source review and non-published-image testing on the candidate SHA.
- **Post-tag obligation:** MUST be re-verified against the actual published rc.7 container image after tagging.

- **Waived by:** Bryant Liu (verified org owner `Chisanan232`), 2026-09-08, via chat approval in the AAASM-6067 release-readiness campaign
- **Condition waived:** J59 `UNTESTED_OR_BLOCKED` (ref: `AAASM-6067-waiver-published-artifact`) — published-artifact fidelity requirement, not verifiable pre-tag
- **Justification:** Registry declares this journey `lifecycle_state: manual_live` with evidence selector pointing at this file — the full operator journey requires a real published gateway image to run end-to-end.
- **Post-tag obligation:** MUST be re-verified as a real manual-live run against the actual published rc.7 artifact after tagging.

**Explicitly NOT waived — do not treat as covered:** J79 was measured
directly this campaign (see its row above), not waived — the founder's
explicit instruction forbade waiving it, and it was not necessary to.

## Verdict

Verdict: PASS

All P0/HIGH-risk surfaces touched by this delta are covered: `aa-runtime`
(J41, CI-verified), the devtool-adapter fail-closed fixes (crate-level,
CI-verified, no journey ID applies), and J79 (real-device,
founder-authorized, evidence recorded above and NOT waived). Every other
selected P0 journey is either unaffected-and-passing or carries the
pre-existing, unchanged AAASM-6067-waiver-published-artifact waiver. All 14
`release_blocking` registry entries this campaign's original selection
missed (J64, J67–J80) are now PASS with fresh, current-candidate evidence —
J64 via a real hardware re-qualification (AAASM-6073, resolved), the
remaining 13 via the `4d9c29959` CI `Test` job, which directly exercises
the AAASM-6091 delta itself. No release-blocking finding is open. AAASM-5276
condition C6's runtime-precedence half remains open on AAASM-5308 as a
residual, honestly disclosed, non-blocking assumption — it is not J79's own
claim and is not required for a truthful PASS here.
