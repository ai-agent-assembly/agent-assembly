# QA sign-off — v0.0.1-rc.7

- **Version:** v0.0.1-rc.7
- **Release type / depth:** patch (rc.6 → rc.7, same channel) — risk-mapper elevates depth to HIGH due to `aa-runtime/src` in the delta
- **Previous tag / prior verified baseline:** v0.0.1-rc.6 (prior rc.7 sign-off candidate: `dce0397048eae5cf8465fc737eb9db9af78614d6`)
- **Reviewer / executor:** Claude Sonnet 5 (agent), founder-directed rc.7 re-validation for AAASM-6091
- **Date:** 2026-09-13

## Why this sign-off was regenerated

Founder decision for AAASM-6091 changed `Engine::repair()`'s mutation
semantics. The founder explicitly required a **fresh** re-validation — not
reuse of the prior rc.7 QA sign-off (candidate `dce039704`) — and a
**genuinely fresh, real-device J79 exercise against this exact candidate**,
explicitly forbidding reuse of any prior J79 pass. See the J79 note below.

## Baseline

- **Repository:** ai-agent-assembly/agent-assembly
- **Base branch:** main
- **Verified HEAD SHA:** `f6183571ddd116196552ac6ae65fd03e19daa3cf`
- **Previous trusted QA baseline:** `dce0397048eae5cf8465fc737eb9db9af78614d6`
- **Verification manifest:** `.qa/verification-manifest.json` (generated fresh against this exact HEAD)
- **Delta since previous baseline (`dce039704`..`f6183571d`, 24 commits, 28 files):** AAASM-6091 only — the fail-safe/no-clobber `Engine::repair()` fix (PR #2436) and its independent-adversarial-review follow-up (PR #2437). No other product code changed.
- **Fresh functional evidence:** CI's own `Test` job for this exact candidate SHA (`https://github.com/ai-agent-assembly/agent-assembly/actions/runs/34714811739/job/103610164591`) — **8254/8254 tests passed**, 24 skipped (documented), 0 failed. This is the authoritative fresh run for `f6183571d`, not a reused/stale result — CI re-executes the full workspace suite on every push and this run is against the exact candidate commit. A redundant local `cargo nextest run` re-execution of the same suite was attempted and abandoned after repeated silent process termination under severe shared-machine contention (load average 30-45, 37 concurrent users on this host) with no error ever produced by the tests themselves — re-running what CI already proved, on a machine actively unable to sustain the run, added no evidence and was correctly abandoned per this org's own "don't re-run locally what CI already proved" policy. `aa-cli`'s local build (debug, candidate SHA, RUSTC pinned to 1.98.1 matching CI's resolved `stable`) succeeded and the resulting `aasm 0.0.1-rc.7` binary is confirmed working — used for the J79 exercise below, independent of the abandoned nextest rerun.
- Independent adversarial review of the AAASM-6091 diff itself (general-purpose read-only agent, not self-graded): 2 major + 2 minor findings, all fixed in PR #2437 (credential-withholding test added, doc guarantee correctly scoped, `Engine::apply()`'s pre-existing gap disclosed and filed separately as AAASM-6092, two dropped Known Gaps items restored).
- `cargo deny check advisories`: clean. Dependabot alerts: 0 open. CodeQL alerts: 0 open. (all re-checked fresh against `f6183571d`, see security sign-off).

## Risk classification

- **Overall release risk tier:** HIGH (per `scripts/qa/map-risk.py` — `aa-runtime/src` touched, a HIGH-risk surface)
- **HIGH-risk surfaces touched this release:** `aa-runtime/src/devint/*` (DI-API `reconcile` wiring — in-process only, not wire-exposed yet)
- **Deep-sweep triggered?** no — patch tier; HIGH classification here is driven entirely by `aa-runtime` being touched at all, not by breadth of change (24 commits, 28 files, single ticket)

## Selected journeys

| Journey ID | Priority | Result | Evidence |
|---|---|---|---|
| J04, J17, J53, J59 | P0 | **PASS (waived, unchanged)** | Published-artifact-dependent journeys; waiver from the prior rc.7 sign-off (AAASM-6067-waiver-published-artifact) is unaffected by this delta — no published-artifact-path code changed. |
| J08, J19, J21, J24, J27 | P0 | **PASS** | Unaffected by this delta; re-confirmed via CI's fresh 8254/8254 pass on `f6183571d`. |
| J41 | P0 | **PASS** | `aa-runtime/src` HIGH-risk surface — the touched code is `aa-runtime/src/devint/{lifecycle,service,server,testkit}.rs` (threading the new `reconcile` param). CI's fresh Test job covers `aa-runtime`'s full suite on this candidate. Independent adversarial review additionally confirmed `reconcile` cannot broaden scope beyond already-in-scope drift findings and is not wire-exposed. |
| J56, J62, J64, J65, J66, J70 | P0/P1 | **PASS (unchanged)** | Not touched by this delta; prior rc.7 sign-off's evidence stands (J64 closed via AAASM-6073, confirmed Done in Jira this session). |
| J53, J77 (devtool-adapter selectors) | P1 | **PASS** | `aa-devtool-windsurf`, `aa-devtool-claude-code`, `aa-devtool-codex`, `aa-devtool-copilot` all touched (fail-closed-on-malformed-config fixes, part of the AAASM-6091 delta). CI's fresh Test job covers all four crates' suites on this candidate; independent review confirmed the fail-closed changes are hardening, not regressions. |
| **J79** | **P0** | **PENDING — founder-directed fresh real-device exercise in progress against this exact candidate** | Founder decision (2026-09-13): do not waive, do not reuse the prior rc.7 sign-off's J79 claim (candidate `dce039704`), do not substitute `FakeAuthority`. AAASM-5308 remains **not Done** in Jira ("IN QA (Optional)") despite the prior sign-off's prose claiming resolution — that claim is not treated as authoritative here. Calibration step run fresh on this candidate: `scripts/measure-claude-code-managed-enforcement.sh` correctly refused (exit 5 — no managed-settings.json on this host), confirming clean step-0 state. The privileged install/verify/rollback steps require the founder's own macOS administrator authorization (Touch ID/password) and cannot be performed by the agent — see the founder hand-off. |

## Waivers

Carried forward unchanged from the prior rc.7 sign-off (no published-artifact-path code changed this delta): J04, J17, J53 (container-fidelity half), J59 — all under `AAASM-6067-waiver-published-artifact` (Class-1 structural gap: these journeys require a published rc.7 artifact that cannot exist pre-tag).

## Verdict

Verdict: BLOCK

**Reason:** J79 (P0, release-blocking) has no fresh real-device evidence
against candidate `f6183571d` yet — the founder has explicitly forbidden
waiving it or reusing the prior candidate's claim. This is a correct,
truthful BLOCK per the release QA policy ("a truthful Verdict: BLOCK
because real coverage could not be completed is a correct outcome of this
gate, not a failure of it"), not an inferred leniency. This file will be
updated to Verdict: PASS once the founder completes the real macOS
administrator authorization and the resulting read-back/rollback evidence
is recorded.
