# QA sign-off — v0.0.1-rc.7

> **PRE-MERGE DRY RUN — NOT A REAL GATE PASS/BLOCK FOR A TAGGED RELEASE.**
> Every AAASM-5819 child PR (#2119-#2130) is still open; none has a
> Pioneer-team approval or is merged to `main`. This dogfood exercises the
> assembled `/release-qa-gate` pipeline (manifest -> risk mapper -> P0
> selection -> sign-off) against the **candidate delta these 10 PRs would
> introduce**, run from a local worktree that merges all 10 branches — it is
> not a review of a tagged `v0.0.1-rc.7`. `rc.7` is used as the version
> label because it is the next unreleased channel per `git ls-remote --tags`
> / `gh release list` at the time of this run (latest real tag: `rc.6`); no
> tag exists for `rc.7` and none is being cut by this run. See AAASM-5831 and
> AAASM-5819 Jira comments for the human-blocker record: a real dogfood on
> the merged base requires these PRs to be reviewed and merged first, which
> needs human (Pioneer-team) action this session cannot perform.

- **Version:** v0.0.1-rc.7 (dry-run label; no tag exists)
- **Release type / depth:** patch (this delta is QA-infrastructure-only —
  Markdown/YAML/shell/Python; no `aa-*` crate source changed)
- **Previous tag / prior verified baseline:** v0.0.1-rc.6 (`8ab5ee8e479aa03c16f27c28fdf16f0d8a9d4804`)
- **Reviewer / executor:** Claude Code session, AAASM-5819 implementation campaign
- **Date:** 2026-08-23

## Baseline

- **Repository:** ai-agent-assembly/agent-assembly
- **Base branch:** main
- **Verified HEAD SHA (real, canonical remote/main at run time):** `ce4638405099011450c920d71e688c10e992ea23`
- **Candidate delta evaluated:** the union of `v0.0.1/AAASM-5820,5822,5823,
  5824,5825,5826,5827,5828,5829,5830/...` branches merged locally
  (`_qa-integration`, not pushed — a local-only integration point used to
  prove wiring, per AAASM-5821's PR description) against `remote/main`.
  33 files changed, 3152 insertions, 2 deletions — see
  `git diff --stat remote/main..._qa-integration` for the exact record.
- **Previous trusted QA baseline:** none — this is the first `/release-qa-gate`
  run; no prior `docs/release/qa-signoff/*.md` exists on `main`.
- **Verification manifest:** generated for real via
  `scripts/qa/build-verification-manifest.sh` — `baseline.source: "unknown"`
  (correct: no prior QA sign-off exists yet), `head_sha` correctly resolved
  to `remote/main`'s real HEAD (not this worktree's uncommitted state).

## Risk classification

- **Overall release risk tier:** HIGH (per `scripts/qa/map-risk.py` run for
  real against the 33-file candidate diff — driven by `scripts/release-
  readiness.sh` matching the `scripts/release-` HIGH rule; `fallback_used:
  true` for several `.claude/agents/*` and `qa/*.md` paths not covered by an
  explicit rule, correctly taking the conservative MEDIUM fallback rather
  than being silently dropped)
- **HIGH-risk surfaces touched this release:** `scripts/release-readiness.sh`
  (release/supply-chain rule)
- **Deep-sweep triggered?** no — patch-tier delta, no major/trust-boundary
  change, cadence not due

## Selected journeys

Full P0 set (mandatory, per AAASM-5820 — unconditional regardless of risk
mapper output):

| Journey ID | Priority | Reason selected | Result |
|---|---|---|---|
| J04 | P0 | always run | `UNTESTED_OR_BLOCKED` — only the source-dev `aasm` build recipe was exercised (AAASM-5830); the public install.sh/Homebrew path this journey actually requires was not attempted this run |
| J08 | P0 | always run | `UNTESTED_OR_BLOCKED` — requires a live gateway + governed-agent walkthrough; not stood up this run |
| J17 | P0 | always run | `UNTESTED_OR_BLOCKED` — requires pulling a published gateway artifact; no such artifact access attempted this run |
| J19 | P0 | always run | `UNTESTED_OR_BLOCKED` — requires a live gateway for author/validate/simulate/apply; not stood up |
| J21 | P0 | always run | `PARTIAL` — real evidence: `mdbook build` of `docs/` succeeded (AAASM-5830, ~4.5s, real run), and every one of the 16 relative cross-references in the new `.claude/skills/release-qa-gate/{SKILL,REFERENCE}.md` was machine-checked to resolve (AAASM-5821 PR description). This covers doc-integrity for the files this delta actually touches, not the full site's full command matrix — scope-limited, not a full PASS |
| J24 | P0 | always run | `UNTESTED_OR_BLOCKED` — requires a live gateway with a policy loaded; not stood up |
| J41 | P0 | always run | `UNTESTED_OR_BLOCKED` — requires exercising SDK+proxy+eBPF together; not attempted |
| J53 | P0 | always run | `UNTESTED_OR_BLOCKED` — requires pulling the published `aa-gateway` container image from GHCR; not attempted (see AAASM-5830's explicit left-out list) |
| J56 | P0 | always run | `UNTESTED_OR_BLOCKED` — requires the full clean-env -> governed-Python-agent flow; not attempted |
| J59 | P0 | always run | `UNTESTED_OR_BLOCKED` — requires the full deploy->observe->govern->recover operator flow; not attempted |

**9 of 10 P0 journeys are `UNTESTED_OR_BLOCKED`.** This is the honest result
of a QA-infrastructure-only delta being dogfooded without the underlying
product infrastructure (live gateway, published artifacts, container
registry access) stood up in this session — not a defect discovered in the
delta itself.

## Lane results

| Lane | Result | Evidence reference |
|---|---|---|
| Functional / config | UNTESTED | No live gateway/CLI-against-gateway exercised |
| Golden journeys | UNTESTED (9/10 P0), PARTIAL (1/10, J21) | See journey table above |
| Design | UNTESTED | No dashboard/browser session stood up this run |
| Reliability | UNTESTED | No induced-failure/recovery scenario exercised |
| Docs / product consistency | PARTIAL | mdbook build + 16/16 skill cross-reference check (real, both executed) |
| Security-relevant behavior | UNTESTED | Out of scope for this QA dogfood — `/release-security-gate` is the independent authority and was not invoked this session |

## Skipped / blocked / untested coverage

| Item | Reason | Risk if unresolved |
|---|---|---|
| J04, J08, J17, J19, J24, J41, J53, J56, J59 (9 P0 journeys) | No live gateway, published-artifact, or container-registry access stood up this session; genuine infrastructure gap, not a discovered defect | HIGH — these are exactly the mandatory always-run journeys; per policy this alone forces BLOCK |
| Real merged-base run | All 10 dependency PRs are open, unreviewed, unmerged — no Pioneer-team approval obtained | Cannot be resolved by this session; requires human review/merge (see blocker note below) |

## Findings

None discovered — this run did not reach behavioral verification of product
surfaces (all 9 blocked journeys are BLOCKED for infrastructure-access
reasons, not because a check ran and failed).

## Known non-blocking issues / residual risk

- The 16-link cross-reference check (J21 partial evidence) covers only the
  new `release-qa-gate` skill's own docs, not a full-site doc-integrity
  sweep — a full J21 run remains outstanding.

## Waivers

None. No human waiver has been sought or granted for the 9 blocked P0
journeys — per the release QA policy, absent an explicit waiver, blocked
mandatory coverage forces `Verdict: BLOCK`. This session does not have
authority to waive mandatory P0 coverage on its own say-so.

## Human blocker (recorded per AAASM-5819 operating rules §"genuinely human-blocked")

**What's blocking**: a real dogfood on the merged base (as AAASM-5831's AC
requires — "the dogfood must use the real merged base") cannot happen until
all 10 dependency PRs (#2119-2129, tracked on AAASM-5819) are reviewed and
merged by a human with Pioneer-team approval authority — this session
cannot approve/merge its own PRs (org governance: "≥1 Pioneer-team approval"
required, and `dev-agent`/this session must not self-merge per the org's
agent-delegation model).

**What was tried**: built and locally verified all 10 pieces individually
(each with real executed tests/negative controls, not just written), merged
them all into a local integration point to prove they wire together (one
expected trivial conflict, resolved), ran the actual manifest generator and
risk mapper against the real candidate diff, and attempted every P0 journey
— 1 of 10 got real partial evidence, the other 9 are honestly blocked on
infrastructure this session did not stand up (live gateway, published
artifacts, container registry) even setting aside the merge-state blocker.

**What decision is needed**: (1) review and merge PRs #2119-2129 in
dependency order (Wave A -> B -> C, per the order used to build them); (2)
after merge, re-run `/release-qa-gate 0.0.1-rc.7` for real against the
merged base with time/infrastructure budgeted to stand up a live gateway
and exercise the SDK/container/published-artifact P0 journeys; (3) decide
whether the real run's likely-partial P0 coverage should be completed
before cutting `rc.7`, or whether `rc.7` proceeds on a different timeline
than this Epic's completion.

## Verdict

<!-- The token `Verdict: PASS` is what release-readiness.sh greps for. This
     run is BLOCK, correctly — 9 of 10 mandatory P0 journeys are unverified
     and unwaived, and no merged base exists yet to genuinely dogfood
     against. This is the gate working as designed, not a defect in it. -->

Verdict: BLOCK
