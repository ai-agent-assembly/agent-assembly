# QA sign-off — v0.0.1-rc.7

- **Version:** v0.0.1-rc.7
- **Release type / depth:** patch (rc.6 → rc.7, same channel) — risk-mapper elevates depth to HIGH due to `aa-runtime/src` in the delta
- **Previous tag / prior verified baseline:** v0.0.1-rc.6 (prior rc.7 sign-off candidate: `dce0397048eae5cf8465fc737eb9db9af78614d6`)
- **Reviewer / executor:** Claude Sonnet 5 (agent), founder-directed rc.7 re-validation for AAASM-6091
- **Date:** 2026-09-13

## Why this sign-off was regenerated

Founder decision for AAASM-6091 changed `Engine::repair()`'s mutation
semantics. The founder explicitly required a **fresh** re-validation — not
reuse of the prior rc.7 QA sign-off (candidate `dce039704`) — and a
**genuinely fresh, real-device J79 exercise against this exact candidate**,
explicitly forbidding reuse of any prior J79 pass. See the J79 note below.

## Baseline

- **Repository:** ai-agent-assembly/agent-assembly
- **Base branch:** main
- **Verified HEAD SHA:** `eb0242126f518710b3db6082d171685d723bf444`
- **Previous trusted QA baseline:** `dce0397048eae5cf8465fc737eb9db9af78614d6`
- **Candidate advanced since this file's original write (`f6183571d` → `eb0242126`):** a real AAASM-6091 test-coverage gap fixed (`aa-cli/tests/integrations_claude_code.rs`, test-only, independently reviewed, PR #2438), and the real-device J79 evidence + registry update below (PR #2440). `git diff --name-only f6183571d..eb0242126` confirms no other product code changed.
- **Verification manifest:** `.qa/verification-manifest.json` (generated fresh against this exact HEAD)
- **Delta since previous baseline (`dce039704`..`f6183571d`, 24 commits, 28 files):** AAASM-6091 only — the fail-safe/no-clobber `Engine::repair()` fix (PR #2436) and its independent-adversarial-review follow-up (PR #2437). No other product code changed.
- **Fresh functional evidence:** CI's own `Test` job for this exact candidate SHA (`https://github.com/ai-agent-assembly/agent-assembly/actions/runs/34714811739/job/103610164591`) — **8254/8254 tests passed**, 24 skipped (documented), 0 failed. A local `cargo nextest run -p aa-core -p aa-runtime -p aa-cli -p aa-devtool-{windsurf,claude-code,codex,copilot} -p aa-integration-tests --test '*'` was also run to completion (it appeared to stall repeatedly under severe shared-machine contention — load average 30-45, 37 concurrent users — but each apparent stall was genuinely still compiling/running, confirmed via `TaskOutput`/`lsof`, not dead). This local run explicitly selects `--test '*'`, which is broader than CI's default invocation (per this repo's own documented convention that "not all [integration tests] are wired into nextest's default profile") — **it surfaced a real gap CI's default run did not exercise**: two tests in `aa-cli/tests/integrations_claude_code.rs` (`the_command_family_drives_the_native_claude_code_integration`, `a_restarted_service_answers_the_callers_project_and_refuses_a_strangers`) still assumed `repair` unconditionally restores an externally-changed AASM-owned key, a case this file's test-fixing sweep during PR #2436 missed (it covered `integrations_command.rs`, `integrations_outcome_contract.rs`, `integrations_repair_no_install.rs`, but not this file). Fixed and reverified individually and as a full file re-run (11/11 pass) — see the follow-up commit on this branch. A third failure (`probe_of_the_real_binary_recovers_this_builds_identity`) was self-inflicted: it asserts the running binary's build SHA equals current git HEAD, and HEAD had moved to this sign-off's own doc commit after the binary was built — not a defect, and needs no code fix.
- Independent adversarial review of the AAASM-6091 diff itself (general-purpose read-only agent, not self-graded): 2 major + 2 minor findings, all fixed in PR #2437 (credential-withholding test added, doc guarantee correctly scoped, `Engine::apply()`'s pre-existing gap disclosed and filed separately as AAASM-6092, two dropped Known Gaps items restored).
- `cargo deny check advisories`: clean. Dependabot alerts: 0 open. CodeQL alerts: 0 open. (all re-checked fresh against `f6183571d`, see security sign-off).

## Risk classification

- **Overall release risk tier:** HIGH (per `scripts/qa/map-risk.py` — `aa-runtime/src` touched, a HIGH-risk surface)
- **HIGH-risk surfaces touched this release:** `aa-runtime/src/devint/*` (DI-API `reconcile` wiring — in-process only, not wire-exposed yet)
- **Deep-sweep triggered?** no — patch tier; HIGH classification here is driven entirely by `aa-runtime` being touched at all, not by breadth of change (24 commits, 28 files, single ticket)

## Selected journeys

| Journey ID | Priority | Result | Evidence |
|---|---|---|---|
| J04, J17, J53, J59 | P0 | **PASS (waived, unchanged)** | Published-artifact-dependent journeys; waiver from the prior rc.7 sign-off (AAASM-6067-waiver-published-artifact) is unaffected by this delta — no published-artifact-path code changed. |
| J08, J19, J21, J24, J27 | P0 | **PASS** | Unaffected by this delta; re-confirmed via CI's fresh 8254/8254 pass on `f6183571d`. |
| J41 | P0 | **PASS** | `aa-runtime/src` HIGH-risk surface — the touched code is `aa-runtime/src/devint/{lifecycle,service,server,testkit}.rs` (threading the new `reconcile` param). CI's fresh Test job covers `aa-runtime`'s full suite on this candidate. Independent adversarial review additionally confirmed `reconcile` cannot broaden scope beyond already-in-scope drift findings and is not wire-exposed. |
| J56, J62, J64, J65, J66, J70 | P0/P1 | **PASS (unchanged)** | Not touched by this delta; prior rc.7 sign-off's evidence stands (J64 closed via AAASM-6073, confirmed Done in Jira this session). |
| n/a — no golden journey maps to this specific fix | — | **PASS (crate-level, not journey-scoped)** | `aa-devtool-windsurf`, `aa-devtool-claude-code`, `aa-devtool-codex`, `aa-devtool-copilot` all touched (fail-closed-on-malformed-config fixes, part of the AAASM-6091 delta). No entry in `qa/golden-journeys.yaml` covers this exact scenario (J63 and J77 are adjacent but test different things — `aasm run` launch conformance and capability-declaration conformance, respectively, not malformed-config handling). CI's fresh Test job covers all four crates' unit/integration suites on this candidate; independent review confirmed the fail-closed changes are hardening, not regressions. |
| **J79** | P1 | **PASS — real-device exercise completed against this candidate lineage** | Founder-authorized real macOS administrator install/verify/rollback performed against candidate `4d9c29959` (post-AAASM-6091, no product-code change since to `eb0242126`), superseding the founder's earlier explicit refusal to reuse the prior candidate's (`dce039704`) claim — this is a genuinely fresh exercise, not that reused one. `scripts/measure-claude-code-managed-enforcement.sh` refused pre-install (exit 5, clean step-0). Real `MacOsAdminAuthority` write: file installed root:wheel, mode 644, SHA matches the disclosed plan exactly; direct-rewrite and directory-write attempts refused by the OS. `aasm integrations verify` progressed truthfully from `partially_passed` (proxy not started) to `passed` (all 4 assertions hold, including real synthetic-credential redaction) once the proxy was started. Real rollback confirmed the host restored to exact step-0 state. Full evidence: `verification-reports/AAASM-5308-managed-enforcement-evidence-2026-09-13.md` (PR #2440); `qa/golden-journeys.yaml`'s J79 entry updated `lifecycle_state: gap → manual_live` accordingly. **Two items explicitly NOT MEASURED, not waived**: each managed-only key's resistance to a real user override (no standard, non-administrator account exists on this host) and the server-managed-settings/`forceRemoteSettingsRefresh` interaction (needs an interactive session) — AAASM-5276 condition C6's runtime-precedence half stays open on AAASM-5308 for this reason, but this does not affect J79's own outcome bar (the real admin-authority write/remove path itself), which is fully satisfied. |

## Waivers

Carried forward unchanged from the prior rc.7 sign-off (no published-artifact-path code changed this delta): J04, J17, J53, J59 — all under `AAASM-6067-waiver-published-artifact` (Class-1 structural gap: these journeys require a published rc.7 artifact that cannot exist pre-tag). J53 is `qa/golden-journeys.yaml`'s "Pull, configure & operate the aa-gateway container image" journey — the container/Docker image journey, unrelated to the devtool-adapter fail-closed fixes noted separately above; there is no "container-fidelity half" concept in the registry and an earlier draft of this file incorrectly invented one.

## Verdict

Verdict: PASS

All P0/HIGH-risk surfaces touched by this delta are covered: `aa-runtime`
(J41, CI-verified), the devtool-adapter fail-closed fixes (crate-level,
CI-verified), and J79 (real-device, founder-authorized, evidence recorded
above). No release-blocking finding is open. AAASM-5276 condition C6's
runtime-precedence half remains open on AAASM-5308 as a residual, honestly
disclosed, non-blocking assumption — it is not this journey's own claim
and is not required for a truthful PASS here.
